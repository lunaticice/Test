if (typeof startPage === "undefined" || startPage === null || startPage === "") { return; }  

var formattedText;

let data = modelContainer45.getData();
let jumlahPage = data.jumlahPage;

if(startPage === jumlahPage) {
    formattedText = false;
} else {
    formattedText = true;
}

return formattedText;