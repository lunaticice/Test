if (typeof currentPage === "undefined" || currentPage === null || currentPage === "") { return; }  

var formattedText; 
let data = modelContainer50.getData();

if(currentPage === data.jumlahPage) {
    formattedText = false;
} else {
    formattedText = true;
}

return formattedText;